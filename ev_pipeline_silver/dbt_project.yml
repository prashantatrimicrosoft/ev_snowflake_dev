name: 'ev_pipeline_silver'
version: '1.0.0'
config-version: 2
profile: 'ev_pipeline_silver'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

models:
  ev_pipeline_silver:
    staging:
      +materialized: view
    intermediate:
      +materialized: ephemeral
    silver:
      +materialized: table
