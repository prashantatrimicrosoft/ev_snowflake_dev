name: 'ev_pipeline_gold'
version: '1.0.0'
config-version: 2
profile: 'ev_pipeline_gold'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

models:
  ev_pipeline_gold:
    +materialized: table
